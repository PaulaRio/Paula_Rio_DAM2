<UserControl x:Class="BasicApp.View.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicApp.View"
             xmlns:viewModel="clr-namespace:BasicApp.ViewModel" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=viewModel:DataViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <ui:DataGrid 
         Grid.Column="1" 
         x:Name="MyDataGrid"       
         ItemsSource="{Binding Films}" 
         AutoGenerateColumns="True" 
         Margin="10" 
         CanUserResizeColumns="True"
         CanUserResizeRows="False"
         VerticalAlignment="Top"
         HorizontalAlignment="Left"
         Foreground="Black"
         BorderBrush="Black"
         MinRowHeight="25"
         MaxHeight="400" 
         ColumnWidth="Auto"
         ScrollViewer.VerticalScrollBarVisibility="Auto"
         ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
        <Border Grid.Column="0" Background="#6A0DAD">
            <StackPanel>
                <!-- Logo -->
                <Border Background="#8E44AD" Padding="10">
                    <TextBlock Text="Gestor Archivos" 
                               Foreground="White" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- Opciones del menú -->
                <ui:Button  
                        Background="Transparent" Foreground="White" 
                        BorderBrush="Transparent" 
                        Margin="10" Padding="10" 
                        HorizontalAlignment="Left"
                        Content="Taquilla"  FontSize="30" >
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Video32" />
                    </ui:Button.Icon>
                </ui:Button >

                <ui:Button 
                        Background="Transparent" Foreground="White" 
                        BorderBrush="Transparent" 
                        Margin="10" Padding="10" 
                        HorizontalAlignment="Left"
                        Content="Personaje" FontSize="30">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ZoomFit20"/>
                    </ui:Button.Icon>
                </ui:Button>

                <ui:Button 
                        Background="Transparent" Foreground="White" 
                        BorderBrush="Transparent" 
                        Margin="10" Padding="10" 
                        HorizontalAlignment="Left"
                        Content="Adjuntar" FontSize="30">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Attach20"/>
                    </ui:Button.Icon>
                </ui:Button>

                <StackPanel VerticalAlignment="Bottom" Margin="10">
                    <ui:Button Background="Transparent" 
                            Foreground="White" 
                            BorderBrush="Transparent" 
                            HorizontalAlignment="Left"
                            Content="Logout"  FontSize="30"  Command="{Binding Logout_ClickCommand}">
                        <ui:Button.Icon>
                            <ui:SymbolIcon Symbol="Settings20"/>
                        </ui:Button.Icon>
                    </ui:Button>
                </StackPanel>
            </StackPanel>
        </Border>
       

    </Grid>
</UserControl>
