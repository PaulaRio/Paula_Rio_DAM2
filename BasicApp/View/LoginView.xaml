<UserControl x:Class="BasicApp.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicApp.View"
             xmlns:viewModel="clr-namespace:BasicApp.ViewModel" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=viewModel:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="MediumBlue">
    <Grid>
        <Border CornerRadius="10" Background="White" Width="300" Height="400" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20">
            <StackPanel>
                <!-- Título -->
                <TextBox Text="Login" FontWeight="Bold" FontSize="24" Foreground="#333" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <!-- Campo de Correo -->
                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10">
                <ui:TextBox PlaceholderText="Enter your email" Height="40" Margin="0,0,0,10" />
                </Border>
                <!-- Campo de Contraseña -->
                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10">
                    <ui:PasswordBox PlaceholderText="Confirm a password" Height="40" Margin="0,0,0,10" />
                </Border>
                <!-- Recordarme y enlace -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Content="Remember me" VerticalAlignment="Center" Foreground="Gray" />
                    <TextBlock Text="Forgot password?" Foreground="#4263EB" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="1" Cursor="Hand"/>
                </Grid>

                <!-- Botón de Login -->
                <ui:Button Content="Login Now" Width="200" Height="40" HorizontalAlignment="Center" Background="#4263EB" Foreground="White" />

                <!-- Enlace para registro -->
                <TextBlock Text="Don't have an account? Signup now" FontSize="12" Foreground="#4263EB" HorizontalAlignment="Center" Margin="0,20,0,0" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
