<UserControl x:Class="BasicApp.View.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicApp.View"
             xmlns:viewModel="clr-namespace:BasicApp.ViewModel" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=viewModel:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="DodgerBlue">
    <Grid>
        <Border CornerRadius="10" Background="White" Width="300" Height="600" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20">
            <StackPanel>

                <TextBox Text="Registration" FontWeight="Bold" FontSize="24" Foreground="#333" Margin="0,0,0,20" HorizontalAlignment="Center"/>


                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:TextBox PlaceholderText="Enter your name" Height="40" Margin="0,0,0,10"  />
                </Border>

                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:TextBox PlaceholderText="Enter your email" Height="40" Margin="0,0,0,10"  />
                </Border>

                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:TextBox PlaceholderText="Create a password" Height="40" Margin="0,0,0,10"  />
                </Border>

                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:TextBox PlaceholderText="Confirm a password" Height="40" Margin="0,0,0,10"  />
                </Border>


                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Content="I accept all terms and conditions" VerticalAlignment="Center" Foreground="Gray" />
                </Grid>

                <ui:Button Content="Register Now" Width="200" Height="40" HorizontalAlignment="Center" Background="#4263EB" Foreground="White" />

                <Button Content="Already have an account? Login now" FontSize="12" Foreground="#4263EB" HorizontalAlignment="Center" Margin="0,20,0,0" Cursor="Hand" Command="{Binding Login_ClickCommand}"/>
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
