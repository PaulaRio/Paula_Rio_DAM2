<UserControl x:Class="PlantillaWPF.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlantillaWPF.View"
             xmlns:viewModel="clr-namespace:PlantillaWPF.ViewModel" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=viewModel:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="DodgerBlue">

    <Grid>
        <Border CornerRadius="10" Background="White" Width="300" Height="400" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20">
            <StackPanel>

                <!--<TextBox Text="Login" FontWeight="Bold" FontSize="24" Foreground="#333" Margin="0,0,0,20" HorizontalAlignment="Center"/>-->
                <Label Content="Login" FontWeight="Bold" FontSize="24" Foreground="#333" Margin="0,0,0,20" HorizontalAlignment="Center" Height="35" Width="90" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>


                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:TextBox  Text="{Binding Email}" PlaceholderText="Enter your email" Height="40" Margin="0,0,0,10"  />
                </Border>

                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10">
                    <ui:PasswordBox PlaceholderText="Confirm a password" Height="40" Margin="0,0,0,10" Password="{Binding Password, Mode=TwoWay}" />
                </Border>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Content="Remember me" VerticalAlignment="Center" Foreground="Gray" />
                    <Button Content="Forgot password?" Foreground="#4263EB" Background="Transparent" BorderThickness="0" Cursor="Hand" Command="{Binding ForgotPasswordCommand}" Grid.Column="1"/>
                </Grid>

                <ui:Button Content="Login Now" Width="200" Height="40" HorizontalAlignment="Center" Background="#4263EB" Foreground="White"  Command="{Binding Data_ClickCommand}"/>

                <ui:Button Content="Don't have an account? Signup now" FontSize="12" Foreground="#4263EB" HorizontalAlignment="Center" Margin="0,20,0,0" Cursor="Hand" Command="{Binding Register_ClickCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
