<UserControl x:Class="PlantillaWPF.View.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlantillaWPF.View"
             xmlns:viewModel="clr-namespace:PlantillaWPF.ViewModel" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=viewModel:RegistrationViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="DarkSeaGreen">
    <Grid>
        <Border CornerRadius="10"  Width="300" Height="473" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20" Background="Ivory">
            <StackPanel>

                <TextBox Text="Registration" FontWeight="Bold" FontSize="24" Foreground="#333" Margin="0,0,0,0" HorizontalAlignment="Center"/>


                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:TextBox Text="{Binding Name}" PlaceholderText="Enter your name" Height="40" Margin="0,0,0,10"  />
                </Border>

                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:TextBox Text="{Binding Email}" PlaceholderText="Enter your email" Height="40" Margin="0,0,0,10"  />
                </Border>

                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:PasswordBox PlaceholderText="Create a password" Height="40" Margin="0,0,0,10"  Password="{Binding Password, Mode=TwoWay}" />
                </Border>

                <Border BorderThickness="0,0,0,2" BorderBrush="LightGray" Margin="0,0,0,10" >
                    <ui:PasswordBox PlaceholderText="Confirm a password" Height="40" Margin="0,0,0,10"  Password="{Binding ConfirmPassword, Mode=TwoWay}" />
                </Border>


                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Content="I accept all terms and conditions" VerticalAlignment="Center" Foreground="Gray" />
                </Grid>

                <ui:Button Content="Register Now" Width="200" Height="40" HorizontalAlignment="Center" Background="DarkSlateGray" Foreground="White" Command="{Binding Register_ClickCommand}" />
                <Button Content="Already have an account? Login now" FontSize="12" Foreground="#4263EB" Cursor="Hand" Command="{Binding Login_ClickCommand}" RenderTransformOrigin="0.49,-0.409"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
