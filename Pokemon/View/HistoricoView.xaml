<UserControl x:Class="Pokemon.View.HistoricoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pokemon.View"
             xmlns:viewModel="clr-namespace:Pokemon.ViewModel" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="LightPink">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        

        <DataGrid 
Grid.Row="1" 
x:Name="MyDataGrid"       
ItemsSource="{Binding Pokemons}" 
AutoGenerateColumns="True" 
Margin="10" 
CanUserResizeColumns="True"
CanUserResizeRows="False"
VerticalAlignment="Top"
HorizontalAlignment="Left"
MinRowHeight="25"
MaxHeight="400" 
ColumnWidth="Auto"
ScrollViewer.VerticalScrollBarVisibility="Auto"
ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
        <Button Command="{Binding SaveToFileCommand}" Grid.Row="1" VerticalAlignment="Bottom" Width="40" Height="40" Margin="15">
            <Image Source="../Resources/descarga.png" Height="40" Width="40"/>
        </Button>
       
    </Grid>
</UserControl>
