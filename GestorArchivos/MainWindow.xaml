<Window x:Class="GestorArchivos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestorArchivos"
        xmlns:view="clr-namespace:GestorArchivos.View"
        xmlns:viewModel="clr-namespace:GestorArchivos.ViewModel" 
        d:DataContext="{d:DesignInstance Type=viewModel:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:InfoViewModel}">
            <view:InfoView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:PrincipalViewModel}">
            <view:PrincipalView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:AbrirFileViewModel}">
            <view:AbrirFileView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu FontSize="15" Grid.Row="0">
            <MenuItem Header="_Info">
                <MenuItem Header="_Info" Command="{Binding SelectViewModelCommand}"
              CommandParameter="{Binding InfoViewModel}"        >
            </MenuItem>
            </MenuItem>
            <MenuItem Header="_File" Click="MenuItem_Click">
                <MenuItem Header="_Guardar"/>
                <MenuItem Header="_Borrar"/>
                <MenuItem Header="_Abrir"
                  Command="{Binding SelectViewModelCommand}"
                  CommandParameter="{Binding FileAbrirViewModel}"/>
            </MenuItem>
           
        </Menu>
        <ContentControl Grid.Row="1" Content="{Binding SelectedViewModel}"/>
    </Grid>
</Window>
