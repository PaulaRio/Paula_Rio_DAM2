<UserControl x:Class="PlantillaWPF.View.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlantillaWPF.View" 
             xmlns:viewModel="clr-namespace:PlantillaWPF.ViewModel"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:view="clr-namespace:PlantillaWPF.View" 
             d:DataContext="{d:DesignInstance Type=viewModel:OverviewViewModel}"
             mc:Ignorable="d" 
             Background="Ivory"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModel:StackPanelViewModel}">
            <view:StackPanelView/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button  Grid.Row="0" Content="Add Grupo" Margin="700,0,0,0"  Command="{Binding AddGrupoCommand}" RenderTransformOrigin="13.16,-6.32"/>
        <Button  Grid.Row="0" Content="Add Autor" Margin="0,0,700,0"  Command="{Binding AddAutorCommand}" RenderTransformOrigin="13.16,-6.32"/>
        <Button  Grid.Row="0" Content="Add Objeto" Margin="150,0,550,0"  Command="{Binding AddObjetoCommand}" RenderTransformOrigin="13.16,-6.32"/>
        <TextBox Grid.Row="0" Margin="300,0,350,0" Text="{Binding IdFiltro, UpdateSourceTrigger=PropertyChanged}" />
        <Button  Grid.Row="0" Content="Filtrar" Margin="450,0,250,0"  Command="{Binding FiltrarCommand}" RenderTransformOrigin="13.16,-6.32"/>
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Items, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5">
                                <StackPanel>
                                    <Button Height="100" Padding="5" Command="{Binding DataContext.SelectViewModelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Id}">
                                        <Image Source="{Binding Photo}" Stretch="Uniform"/>
                                    </Button>
                                    <Border Background="White" Height="50">
                                        <TextBlock Text="{Binding Name}" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <ContentControl Grid.Row="2" Content="{Binding SelectedViewModel}"/>
    </Grid>
</UserControl>
