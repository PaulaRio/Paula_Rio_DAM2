<UserControl x:Class="PlantillaWPF.View.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlantillaWPF.View"
             xmlns:viewModel="clr-namespace:PlantillaWPF.ViewModel" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModel:DataGridViewModel}"
             Background="DarkSeaGreen">
    <Grid>
        <Grid.ColumnDefinitions>


        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ui:Button Content="Add" HorizontalAlignment="Right" Grid.Column="1" Foreground="White" 
               Background="DarkSlateGray" Margin="0,18,40,0" VerticalAlignment="Top"   Command="{Binding Add_ClickCommand}" Height="30" Width="60"/>
        <ui:DataGrid
         Grid.Row="1"
         x:Name="MyDataGrid"       
         ItemsSource="{Binding Objects}" 
         AutoGenerateColumns="True" 
        AutoGeneratingColumn="MyDataGrid_AutoGeneratingColum" 
        Margin="0,10,0,0" 
        CanUserResizeColumns="True"
        CanUserResizeRows="False"
        VerticalAlignment="Top"
        HorizontalAlignment="Center"
        Foreground="Black"
        Background="Ivory"
        BorderBrush="Black"
        MinRowHeight="25"
        MaxHeight="400" 
        ColumnWidth="Auto"
        ScrollViewer.VerticalScrollBarVisibility="Auto"
        ScrollViewer.HorizontalScrollBarVisibility="Auto" Height="340" Width="780"/>
        <ui:Button Content="Import" HorizontalAlignment="Left"  Grid.Row="2" Foreground="White" 
        Background="DarkSlateGray" Margin="49,0,0,0" VerticalAlignment="Center"   Command="{Binding ImportCommand}" Height="30" Width="60"/>
        <ui:Button Content="Export" HorizontalAlignment="Left"  Grid.Row="2" Foreground="White" 
        Background="DarkSlateGray" Margin="700,0,0,0" VerticalAlignment="Center"   Command="{Binding ExportCommand}" Height="30" Width="60"/>



    </Grid>


</UserControl>
