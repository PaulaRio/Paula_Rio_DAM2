<UserControl x:Class="Examen.View.PrincipalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Examen.View"
             xmlns:view="clr-namespace:Examen.View"
             xmlns:viewModel="clr-namespace:Examen.ViewModel" d:DataContext="{d:DesignInstance Type=viewModel:VentanaViewModel}"
             mc:Ignorable="d" 
             Background="LightCyan"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--<StackPanel >-->
            <!--<ItemsControl ItemsSource="{Binding Items, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5">
                            <StackPanel>
                                <Image Source="{Binding  ImageFileDirectory}" Stretch="Uniform" Height="100"/>
                                <TextBlock Text="{Binding NameFileDirectory}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        </ScrollViewer>
        <Grid.ContextMenu>
            <ContextMenu >
                <MenuItem Name="Carpeta" Header="Crear carpeta" Command="{Binding OpenCreateDirectoryCommand}" />
                <MenuItem  Name="Fichero" Header="Crear fichero" Command="{Binding OpenCreateFileCommand}" />
            </ContextMenu>
        </Grid.ContextMenu>-->
    </Grid>
</UserControl>
