<UserControl x:Class="Examen.View.Ventana2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Examen.View"
             xmlns:view="clr-namespace:Examen.View"
             xmlns:viewModel="clr-namespace:Examen.ViewModel" d:DataContext="{d:DesignInstance Type=viewModel:Ventana2ViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="LightSteelBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DataGrid 
 Grid.Row="1"
x:Name="MyDataGrid"       
ItemsSource="{Binding Planetas}" 
AutoGenerateColumns="True" 
Margin="10" 
CanUserResizeColumns="True"
CanUserResizeRows="False"
VerticalAlignment="Top"
HorizontalAlignment="Left"
MinRowHeight="25"
MaxHeight="400" 
ColumnWidth="Auto"
ScrollViewer.VerticalScrollBarVisibility="Auto"
ScrollViewer.HorizontalScrollBarVisibility="Auto" RenderTransformOrigin="13.75,5.385"/>
    </Grid>
</UserControl>
